<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="main_style_img">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/x50</item>
        <item name="android:layout_marginTop">@dimen/x15</item>
    </style>

    <style name="main_style_text">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginBottom">@dimen/x10</item>
        <item name="android:layout_marginTop">@dimen/x5</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/x25</item>
    </style>


    <style name="main_menu">
        <!-- 主界面更多菜单动画 -->
        <item name="android:windowEnterAnimation">@anim/main_menu_in</item>
        <item name="android:windowExitAnimation">@anim/main_menu_out</item>
    </style>

    <!-- dialog样式 -->
    <style name="MyDialog" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
